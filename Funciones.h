/** \brief
 *
 * \param int[] variable donde guardo los valores del vector
 * \param int variable para la cantidad de numeros a ingresar
 *
 */
void cargarVector(int[], int);


/** \brief
 *
 * \param int[] variable para que recorra los valores guardados del vector
 * \param int variable para que indica la cantidad de numeros ingresados
 *
 */
void mostrarNumero(int[], int);


/** \brief
 *
 * \param int[] variable para que recorra los valores guardados del vector buscando el mayor
 * \param int variable para que indica la cantidad de numeros ingresados
 * \return int el numero mayor encontrado
 *
 */
int buscarMaximo(int[], int);


/** \brief
 *
 * \param [] int variable para que recorra los valores guardados del vector
 * \param int variable para que indica la cantidad de numeros ingresados
 * \param int variable donde guardo el indice donde se encuentra el numero
 * \return int indice donde se encuentra el numero o -1 si no se encuentra
 *
 */
int buscarValor(int [],int,int);
